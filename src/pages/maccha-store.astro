---
// src/pages/maccha-store.astro
import ProductCard from '../components/ProductCard.astro';

// 假設這是從你的資料來源取得的
const BASE_REPO_URL = "https://your-repo-url.com";
const products = [ /* ... 你的商品陣列 ... */ ];
---

<div class="grid grid-cols-1 md:grid-cols-3 gap-6">
  {products.length > 0 ? (
    products.map((item) => (
      <ProductCard product={item} BASE_REPO_URL={BASE_REPO_URL} />
    ))
  ) : (
    <p class="text-gray-400 text-center col-span-full py-8">尚無品項</p>
  )}
</div>

<script is:inline>
  function addToCart(id, name, p1, p2, stock, limit) {
    // 你的購物車 JS 邏輯
  }
  function adjQty(id, delta) {
    // 你的數量加減 JS 邏輯
  }
</script>
